import 'package:flutter/material.dart';

class HomeScreenSearchModal extends StatelessWidget {
  const HomeScreenSearchModal({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: double.infinity, child: Column(children: [
      
    ],),);
  }
}
